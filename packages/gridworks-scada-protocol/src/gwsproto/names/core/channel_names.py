class CoreChannelNames:
    # the layout's single transactive-power DerivedChannel (the metered-power
    # aggregate the power-meter actor computes from its declared CT inputs)
    transactive_power = "transactive-power"
