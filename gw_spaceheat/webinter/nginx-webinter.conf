# Nginx configuration for GridWorks SCADA Web Interface
# Add this to your existing nginx configuration

# WebSocket proxy for the SCADA web interface
location /ws {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # WebSocket specific settings
    proxy_read_timeout 86400;
    proxy_send_timeout 86400;
    proxy_connect_timeout 60;
    
    # Disable buffering for real-time communication
    proxy_buffering off;
    proxy_cache off;
}

# Optional: Serve the HTML file directly from nginx (alternative to GitHub Pages)
# Uncomment this if you want to serve the HTML from your EC2 instance instead of GitHub Pages
# location /scada {
#     alias /home/ubuntu/gridworks-scada/gw_spaceheat/webinter/index_production.html;
#     try_files $uri $uri/ =404;
# }
